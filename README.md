# angular-express-rest-apidemo


A simple angular application running on express server, with rest api.
To run this demo, ensure that you have node installed. The following commands in bash or command prompt,

$ npm install

To run

$ node server.js
